package com.iteratrlearning.design_patterns.problems.pipes;

import java.util.Arrays;

import static java.util.stream.Collectors.joining;

//TODO
public class GrepCommand
{

    private String word;

    public GrepCommand(String word)
    {
        //TODO
    }


    protected String process(String input)
    {
        // TODO: should filter only lines (split by \n) that contain the word passed as argument
        // TODO: should return a single joined string of the selected lines
        return null;
    }
}
